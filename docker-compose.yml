version: "3" 

services: 
  web-server:
    image: cjswosa22/web-server:0.0.1
    environment: 
      - NGINX_HOST=0.0.0.0 
      - NGINX_PORT=80 
    ports: 
      - 8080:80

    build: 
      dockerfile: ./dockerfiles/web-server.Dockerfile